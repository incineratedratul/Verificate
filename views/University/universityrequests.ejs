<!DOCTYPE html>
<html>
  <head>
    <title>University Requests</title>
   <style>
    nav {
        background-color: #0077B5;
        color: white;
        padding: 30px 0;
      }
      
      nav ul {
        display: flex;
        justify-content: center;
        align-items: center;
        list-style: none;
        margin: 0;
        padding: 0;
      }
      
      nav li {
        margin-right: 30px;
      }
      
      nav a {
        color: white;
        text-decoration: none;
      }
      
      
      h1 {
        text-align: center;
        margin-bottom: 30px;
      }
      
     
      table {
        border-collapse: collapse;
        margin: auto;
      }
      
      thead {
        background-color: #0077b5;
        color: white;
      }
      
      th, td {
        padding: 12px;
        text-align: center;
        border: 1px solid #ddd;
      }
      
      th {
        font-size: 18px;
      }
      
      td {
        font-size: 16px;
      }
      
      td:nth-child(6) {
        font-weight: bold;
      }
      
      .approveBtn, .rejectBtn {
        padding: 8px 16px;
        margin-right: 8px;
        font-size: 14px;
        background-color: #0077b5;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }
      
      .approveBtn:hover, .rejectBtn:hover {
        background-color: #005d8f;
      }
      
   </style>
</head>
<body>
  <nav>
    <ul>
      <li><a href="/universityrequests">View Requests</a></li>
      <li><a href="universityacceptedrequest">Accepted Requests</a></li>
      <li><a href="universitynotifications">Notifications</a></li>
      <li><a href="/login">Logout</a></li>
    </ul>
  </nav>
  <h1>University Requests</h1>
  <table>
    <thead>
      <tr>
        <th>Request ID</th>
        <th>Student Name</th>
        <th>Student ID</th>
        <th>Certificate Name</th>
        <th>File</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>001</td>
        <td>Sumaiya Islam Mouno</td>
        <td>1001</td>
        <td>Bachelor's Degree</td>
        <td><a href="#">Download</a></td>
        <td>
          <button class="approve-button">Approve</button>
          <button class="reject-button">Reject</button>
        </td>
      </tr>
      <tr>
        <td>002</td>
        <td>Tasfia Rahman</td>
        <td>1002</td>
        <td>Transcript</td>
        <td><a href="#">Download</a></td>
        <td>
          <button class="approve-button">Approve</button>
          <button class="reject-button">Reject</button>
        </td>
      </tr>
    </tbody>
  </table>

    
    <script>

        // Select all delete buttons
const deleteButtons = document.querySelectorAll('.delete-button');

// Add event listener to each delete button
deleteButtons.forEach((button) => {
  button.addEventListener('click', (event) => {
    // Confirm deletion
    const confirmation = confirm('Are you sure you want to delete this request?');

    if (confirmation) {
      // Get the row of the button clicked
      const row = event.target.parentNode.parentNode;

      // Remove the row from the table
      row.parentNode.removeChild(row);
    }
  });
});


  </script>
  </body>
</html>